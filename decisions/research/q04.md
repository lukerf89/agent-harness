# Q04 · Scheduler · Budget primitives

## Research findings

The state of the art for LLM budget controls in 2025-2026 reveals a striking gap: most major providers have moved AWAY from hard spending caps toward soft alerts, leaving production agent operators to roll their own enforcement. OpenAI quietly changed monthly budget limits from hard cutoffs to alerts-only in 2025, leaving developers to layer proxies (LiteLLM, custom gateways) for actual ceilings. Anthropic exposes workspace-scoped spend limits but documents them as "spend notifications" rather than hard kills, with the one exception being the Claude Code workspace, which uniquely supports per-user monthly spend limits. Vercel AI Gateway provides team budgets ($200 default) with 50/75/100% webhook notifications, but explicitly notes that exceeding the limit does NOT automatically stop usage unless a separate pause-projects toggle is enabled.

The runaway-cost incidents driving 2025-2026 design thinking are vivid: a $607 Replit Agent bill from an unattended autonomous session; a multi-agent loop that scaled from $127/week to $47,000/week in eleven days because two agents recursively asked each other for clarification. These have crystallized a consensus in the agentic AI literature: budgets must be MULTI-DIMENSIONAL (cost in dollars, tokens, wall-clock, step count, and tool calls) because each catches a different failure mode — token budgets prevent runaway prompts, step budgets prevent infinite loops, time budgets prevent stuck workflows, and cost budgets prevent cascading spend. Single-dimension caps consistently miss real-world failures.

Workflow runtimes (Inngest, Trigger.dev, Hatchet, Temporal) handle ADJACENT concerns well — concurrency keys for fair multi-tenant scheduling, throttling per-user via expression keys, step-level retries — but none of them natively enforce dollar/token budgets. Inngest's `step.ai.infer` adds telemetry but the cost guard still lives in your code. The most sophisticated open-source budget enforcement lives in LLM proxies: LiteLLM exposes `max_budget`, `soft_budget`, and `budget_duration` per key, per team, and per end-user, with overlapping windows ($10/day AND $100/month on the same key), hard-rejecting requests with `ExceededBudget` when exceeded.

The emerging pattern from production circuit-breaker designs is a three-layer gateway: a token-bucket throttler for volume, circuit breakers triggered by cost-per-request thresholds, and a fallback chain that gracefully degrades (smaller/cheaper model, disabled tools, cached responses, reduced reasoning depth) rather than failing hard. Cloud-cost UX precedent (AWS Budgets) standardizes graduated alert thresholds at 50/75/90/100% with both actual and forecasted signals, routed to high-signal channels (Slack, PagerDuty) rather than email alone, and supports auto-actions (stop instances, apply IAM policies) at hard thresholds.

## Options

### Option 1: Single-Dimension Dollar Cap (Replit-style)
**Summary:** Each scheduled job declares a USD ceiling; harness kills the job when cost-to-date exceeds it.
**How it works:** When a watcher fires, the harness computes cost in real time from per-model pricing tables and aborts the job if cumulative spend crosses the cap. Caps stack at job, day, and workspace scopes — the tightest binding wins. User UI shows one number per scope.
**Pros:**
- Simple mental model for non-technical knowledge workers
- Maps directly to bill-shock prevention (the actual user fear)
- Easy to defaults and document ("$0.50 per watcher run, $5/day per workspace")
**Cons:**
- Misses infinite-loop and stuck-tool failures that haven't burned cash yet but will
- Single number obscures runaway-token vs runaway-toolcall failure signatures
- Real-time cost calc requires provider pricing tables that drift

### Option 2: Multi-Dimensional Execution Budget (BudgetGuard/TimeoutGuard pattern)
**Summary:** Every job carries a typed budget tuple — {dollars, tokens, wall_clock_seconds, model_calls, tool_calls} — and the first dimension to exhaust ends the job.
**How it works:** Job spec declares limits per dimension; harness tracks each independently and emits structured "budget pressure" warnings to the model when 75% of any dimension is spent (giving the agent a chance to wrap up cleanly). Scopes layer (per-job, per-user/day, per-workspace/month). On exhaustion, the harness checkpoints state, posts a notification, and offers a one-click "extend budget and resume" to the user.
**Pros:**
- Catches all four documented failure modes (runaway prompt, infinite loop, stuck tool, cascading spend)
- Pressure-warning UX lets the agent finish the task instead of failing mid-stream
- Maps cleanly to provenance/draft artifacts — partial drafts survive budget exhaustion
**Cons:**
- More knobs means more decisions for the user (must hide behind smart defaults)
- Implementing pressure-injection requires harness-side prompt manipulation
- Tracking five dimensions per job adds bookkeeping overhead

### Option 3: Tiered Budget with Graceful Degradation (3-layer gateway)
**Summary:** Soft caps trigger model downgrade or tool-pruning before hitting hard kill ceilings.
**How it works:** Three thresholds per scope. At 50% spend: notify-only. At 75%: degrade — switch from Opus to Sonnet (or Sonnet to Haiku), disable expensive tools, cap reasoning depth, prefer cached/templated responses. At 100%: hard kill with checkpoint. Inspired by AWS Budgets actions plus the circuit-breaker literature.
**Pros:**
- Preserves user experience during budget squeeze instead of failing outright
- Aligns with knowledge-work expectations (a partially-degraded research synthesis beats no synthesis)
- Aligns with cost-aware routing patterns already used by AI gateways
**Cons:**
- Model downgrade can silently degrade output quality without user awareness
- Degradation paths must be tested per workflow type (drafting vs triage have different fallbacks)
- More state machine complexity per scheduled job

### Option 4: Proxy-Enforced Dollar+Token with Job-Local Step Caps
**Summary:** Borrow LiteLLM's per-key dollar+token model for spend, layered with harness-local step and wall-clock limits per job invocation.
**How it works:** Each user/workspace gets a virtual API key with `max_budget` (USD), `soft_budget` (alert), `budget_duration` (per day/month), and TPM/RPM limits. Per-scheduled-job specs additionally declare `max_steps`, `max_wall_clock`, and `max_tool_calls` enforced inside the harness. Spend control is provider-agnostic; flow control is harness-native.
**Pros:**
- Cleanly separates "money safety" (proxy) from "control flow safety" (harness)
- Reuses battle-tested LiteLLM enforcement instead of reimplementing
- Per-key model gives clean per-workspace/per-user isolation that survives harness bugs
**Cons:**
- Two enforcement layers means two places to debug when a job is killed
- Adds a hard proxy dependency in the request path (latency, reliability)
- Doesn't natively support degradation — kills are binary

## Recommendation
**Pick:** Option 2 — Multi-Dimensional Execution Budget — extended with Option 3's graceful-degradation tier.

Knowledge-work scheduled jobs (watch inbox, monitor news on topic X, draft weekly digest) are uniquely vulnerable to all four failure modes: a draft generator can token-bloat, a research watcher can tool-call-loop on a slow source, a triage job can wall-clock-hang on a flaky webhook. A single-dollar cap (Option 1) reproduces the Replit failure mode where the cap fires AFTER the damage. A proxy-only model (Option 4) misses step/loop failures and conflates "money safety" with "the work was actually useful" — for knowledge work, a $0.30 watcher that loops 400 times and produces nothing is worse than a $0.30 watcher that produces a partial draft. The multi-dimensional budget plus pressure-warning gives the model the chance to checkpoint a draft artifact before exhaustion, which compounds with your novel "draft as first-class versioned artifact" primitive.

The UX surface should be opinionated: ship two preset tiers (Light: $0.10/run, 30s, 20 steps; Heavy: $1/run, 5min, 100 steps), expose a one-line override per watcher, enforce a workspace-level monthly hard ceiling that defaults to $20 (the bill-shock floor most consumer users tolerate), and emit 50/75/100% notifications via the same channel the user receives drafts. At 75% workspace spend, auto-downgrade to a cheaper model class and notify; at 100%, pause all watchers and require explicit reactivation. Make extension a single tap from the alert — the AWS Budgets lesson is that friction-free extension prevents users from disabling budgets entirely.

## Sources
- [Anthropic Workspaces Documentation](https://platform.claude.com/docs/en/build-with-claude/workspaces)
- [OpenAI API Budget Limits: Control Spend Before Calls (SatGate)](https://satgate.io/blog/how-to-add-budget-limits-to-openai-api-calls)
- [Vercel Spend Management Documentation](https://vercel.com/docs/spend-management)
- [LiteLLM Budgets & Rate Limits](https://docs.litellm.ai/docs/proxy/users)
- [LiteLLM Team Budgets](https://docs.litellm.ai/docs/proxy/team_budgets)
- [Inngest Concurrency Management](https://www.inngest.com/docs/guides/concurrency)
- [AWS Budgets Best Practices](https://docs.aws.amazon.com/cost-management/latest/userguide/budgets-best-practices.html)
- [The $607 Replit Bill — Avoiding Runaway AI Costs (Vibe Coder Blog)](https://blog.vibecoder.me/607-replit-bill-avoiding-runaway-ai-costs)
- [Execution Budgets for Autonomous Systems (Rack2Cloud)](https://www.rack2cloud.com/ai-inference-execution-budgets/)
- [Rate Limiting AI Agents: 3-Layer Gateway (TrueFoundry)](https://www.truefoundry.com/blog/rate-limiting-ai-agents-preventing-llm-api-exhaustion)
- [Circuit Breaker Patterns for AI Agent Reliability (Hendricks)](https://brandonlincolnhendricks.com/research/circuit-breaker-patterns-ai-agent-reliability)
- [Agent Runaway Costs: How to Set LLM Budget Limits (RelayPlane)](https://relayplane.com/blog/agent-runaway-costs-2026)
